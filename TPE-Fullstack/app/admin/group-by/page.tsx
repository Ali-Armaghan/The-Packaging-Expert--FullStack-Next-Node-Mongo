import { AdminPlaceholder } from "@/components/admin";

export default function AdminGroupByPage() {
  return (
    <AdminPlaceholder
      title="Group By"
      description="Manage catalog grouping options used to organize products (material, finish, size, and more)."
    />
  );
}
