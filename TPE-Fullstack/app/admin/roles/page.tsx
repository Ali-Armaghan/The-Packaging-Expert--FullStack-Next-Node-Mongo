import { AdminPlaceholder } from "@/components/admin";

export default function AdminRolesPage() {
  return (
    <AdminPlaceholder
      title="Roles"
      description="Define roles and permissions for the admin panel."
    />
  );
}
